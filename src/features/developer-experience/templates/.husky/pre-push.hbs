#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

{{#if enableTests}}
echo "🧪 Running tests..."
pnpm test
{{/if}}

{{#if enableBuild}}
echo "🔨 Running build..."
pnpm build
{{/if}}

echo "✅ Pre-push checks completed!"